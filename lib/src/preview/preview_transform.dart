// Pure aspect-ratio / cover-contain / letterbox layout math.
// Shared by CustomCameraPreview and the annotation coordinate mapping (§9)
// so preview layout and overlay layout can never disagree.
// See design doc §8, §9.
