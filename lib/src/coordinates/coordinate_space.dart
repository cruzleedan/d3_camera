// Named coordinate spaces + conversion functions between them: widget
// pixels, normalized image space, captured-image pixels, export-canvas
// pixels. See design doc §9 for the full transform pipeline.
