// Annotation: sealed, immutable base class + variants (Rectangle, Circle,
// Line, Arrow, Freehand, Text, Highlight). Geometry is always normalized
// [0,1] image-space (see coordinates/), never widget or sensor pixels.
// See design doc §4, §9, §10.
